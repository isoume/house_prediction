FROM bitnami/kafka:4.0.0

# Exposer le port Kafka par défaut
EXPOSE 9092

# (Optionnel) Copier ta configuration personnalisée si besoin
# COPY my-custom-config/server.properties /opt/bitnami/kafka/config/server.properties
